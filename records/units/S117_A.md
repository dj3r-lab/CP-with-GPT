# S117-A — Rollback Union-Find (DSU with Undo)

> Status: Hypothetical/Test Learning Unit
> Actual curriculum impact: None
> Mastery/Review Debt impact: None
> Purpose: Test the Learning Unit presentation and per-response textbook/GitHub synchronization workflow.

## Response 1 — Parts A-D

### Topic
Rollback Union-Find (DSU with Undo).

The S117-A label and Stage 117 placement are fictional. The algorithmic content itself is real competitive-programming material.

### Part A — Position and prerequisites
Assumed prerequisites:
- ordinary DSU
- parent/root representation
- union by size/rank
- vector/stack history
- basic logarithmic/amortized complexity

Core Decision Boundaries:
1. ordinary DSU vs rollback DSU
2. path compression vs rollback-friendly non-compressing find
3. full-state snapshot copies vs change logging

### Part B — Objectives
The learner should be able to:
- explain why rollback changes the DSU design;
- implement DSU rollback without path compression;
- use history size as a snapshot token;
- restore parent/size state correctly;
- analyze find/unite/snapshot/rollback complexity;
- compare full-copy snapshots with change logs.

### Part C — Core content
- A successful union changes only a constant number of fields if path compression is disabled.
- Store the child root, parent root, and parent root's previous size in history.
- Use union by size to keep tree height O(log N).
- `snapshot()` returns the current history size.
- `rollback(token)` undoes history entries in LIFO order until the stored size is restored.
- Complexity: find O(log N), unite O(log N), snapshot O(1), one successful-union undo O(1), rollback O(K) for K undone unions, storage O(N+U).

### Part D — Worked Example
Problem: temporary network links with LINK / SAVE / RESTORE / SAME operations.
The worked example demonstrates saving a history-size token, adding temporary connectivity, checking it, rolling back, and verifying that connectivity returns exactly to the saved state.

### Persistent artifacts
- `textbook/Main_Contents/S117_A_Learning_Unit.tex`
- `textbook/Appendix/S117_A_Appendix.tex`

### Assessment state
Part E has not been presented. No formal assessment exists yet.
